services:
  postgres:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
  minio:
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
  api:
    ports:
      - "${API_PORT:-8000}:8000"
  frontend:
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
